<UserControl x:Class="CSPDS.Views.PlotSettingsList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CSPDS.Views"
             xmlns:source="clr-namespace:CSPDS"
             mc:Ignorable="d" Height="600" Width="800">

    <Grid Margin="3">
        <DataGrid x:Name="dgSettings" AutoGenerateColumns="False" d:DataContext="{d:DesignInstance Type=source:PlotSettingsDescriptor}">
            <DataGrid.Columns>
                <DataGridTextColumn IsReadOnly="True" Binding="{Binding Name}"/>
                <DataGridTextColumn IsReadOnly="True" Binding="{Binding PageSizeName}"/>
                <DataGridTextColumn IsReadOnly="True" Binding="{Binding PlotterName}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>