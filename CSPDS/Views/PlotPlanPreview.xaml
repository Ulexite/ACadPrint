<Window x:Class="CSPDS.Views.PlotPlanPreview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CSPDS.Views"
             xmlns:source="clr-namespace:CSPDS"
             mc:Ignorable="d" Height="600" Width="800">

    <Grid Margin="3">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <DataGrid x:Name="dgSettings" AutoGenerateColumns="False" d:DataContext="{d:DesignInstance Type=source:PlotSettingsDescriptor}" Grid.Row="2">
            <DataGrid.Columns>
                <DataGridTextColumn IsReadOnly="True" Header="Название" Binding="{Binding Name}"/>
                <DataGridTextColumn IsReadOnly="True" Header="Размер страницы" Binding="{Binding PageSizeName}"/>
                <DataGridTextColumn IsReadOnly="True" Header="Имя плоттера" Binding="{Binding PlotterName}"/>
            </DataGrid.Columns>
        </DataGrid>
        
    </Grid>
</Window>